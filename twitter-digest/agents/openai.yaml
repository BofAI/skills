interface:
  display_name: "Twitter Digest"
  short_description: "Analyze X mentions, trends, DMs, daily summaries, and local memory"
  default_prompt: "Use $twitter-digest to summarize my X mentions, timeline hotspots, important DMs, and what changed since prior runs."
