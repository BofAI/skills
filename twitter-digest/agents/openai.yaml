interface:
  display_name: "Twitter Digest"
  short_description: "Generate X/Twitter daily digests from own account"
  default_prompt: "Use $twitter-digest to generate my X daily digest, including mentions, timeline hotspots, important DMs, and action items."
