interface:
  display_name: "Twitter Digest"
  short_description: "Generate X digests and switch authorized accounts"
  default_prompt: "Use $twitter-digest to generate my read-only Chinese X daily digest or securely switch its active X account with verified OAuth and account-specific Chat key recovery."
