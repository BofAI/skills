interface:
  display_name: "Twitter Digest"
  short_description: "Analyze X timeline, mentions, posts, searches, and visible DMs through X MCP plus browser DM collection"
  default_prompt: "Use $twitter-digest to summarize my X timeline, mentions, own posts, searches, and important visible DMs."
