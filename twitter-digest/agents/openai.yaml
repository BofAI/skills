interface:
  display_name: "Twitter Digest"
  short_description: "Generate read-only X and encrypted Chat daily digests with xurl"
  default_prompt: "Use $twitter-digest to generate my concise Chinese X daily digest with the bundled BofAI xurl, an exact local 24-hour window, ID-based Chat direction, both sides preserved chronologically, no public engagement metrics, no drafted or suggested replies, no collection diagnostics, no exposed secrets, and no X write actions."
