interface:
  display_name: "X/Twitter MCP"
  short_description: "Install X MCP and generate MCP-based digests"
  default_prompt: "Use $twitter-mcp to generate my X daily digest from X MCP tools, or install and register X MCP if tools are missing."
